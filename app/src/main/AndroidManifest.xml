<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sha.kamel.sample">

    <application
        android:name=".util.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.Activity1"/>
        <activity android:name=".ui.activity.Activity2"/>
        <activity android:name=".ui.activity.Activity3"/>
        <activity android:name=".ui.activity.Activity4"/>
        <activity android:name=".ui.activity.Activity5"/>
        <activity android:name=".ui.activity.Activity6"/>
        <activity android:name=".ui.activity.Activity7"/>

    </application>

</manifest>